refactor: memoize Movie component and add lazy loading for HeroSection

- Wrap Movie component with memo() to prevent unnecessary re-renders
- Add lazy loading for HeroSection component
- Add useMemo for moviesData array to prevent recalculation
- Add useCallback for handleRetry to stabilize function reference
- Add Suspense fallback with LoadingFallback for better UX
